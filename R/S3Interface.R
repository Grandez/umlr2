# `+.UMLR2`:
#
# `+.UMLR2` <- function(x, y) {
#     # Coger y
#     # Poner lo primero en mayusucla y2
#     # evaluar paste0(x$add, y2)
#     n <- attr(x, "modulus")
#     x <- unclass(x)
#     y <- unclass(y)
#     modulus(x + y, n)
# }
